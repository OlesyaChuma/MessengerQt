; Пример конфигурации сервера MessengerQt.
; Скопируй этот файл рядом как server.ini и подставь свои значения.

[Database]
host=localhost
port=5432
name=messenger_db
user=messenger_user
password=messenger_pass_2026

[Server]
listen_host=0.0.0.0
listen_port=54000
max_attachment_size_mb=10

[Logging]
log_file=server.log
log_level=info